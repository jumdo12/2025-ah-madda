spring:
  main:
    web-application-type: none
  scheduling:
    enabled: true

mail:
  worker:
    enabled: true
  outbox:
    rabbitmq:
      enabled: true

notification:
  scheduler:
    enabled: true
